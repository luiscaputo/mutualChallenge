import { Account } from "../models/Account";

export interface IUsers {
    id?: Account,
    nome?: Account,
    cpf?: Account,
    phone?: Account,
    adress?: Account,
    status?: Account,
    created_at?: Account,
    disabled_at?: Account
}